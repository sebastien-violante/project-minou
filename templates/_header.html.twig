<section id="sticky">
    <header id="navbar" class="navi">
        {% if app.user and app.user.roles.0 in ['ROLE_ADMIN'] %}
        <div class="dropdown">
                <button>Administrer</button>
                <div class="content">
                    <a href="{{ path('new-article') }}">Articles</a>
                    <a href="">Utilisateurs</a>
                </div>
        </div>
        {% endif %}
        <a href="{{ path('home') }}">Accueil</a>
        {% if app.user %}
        <div class="dropdown">
            <button>Chats</button>
            <div class="content">
                <a href="{{ path('new_cat') }}">Nouveau</a>
                <a href="{{ path('cat-list') }}">Liste</a>
            </div>
        </div>
        {% endif %}
        <a href="{{ path('app_cgu') }}">C.G.U.</a>
        <a href="{{ path('contact') }}">Contact</a>
        {% if app.user %}
            <a href="{{ path('logout') }}">Déconnexion</a>
            {% else %}
            <a href="login">Connexion</a>
        {% endif %}
        <a class="icon" onclick="navDeploy()">&#9776;</a>   
    </header>
    <section id="logo">
        <img class="header-cat-picture" src="/pictures/bluecat.svg"</img>
        <p>MIN<span>OU ?</span></p>
  </section>
</section>

<script>
    function navDeploy() {
        var x = document.getElementById("navbar");
        if(x.className === "navi") {
            x.className += " responsive";
            } else {
            x.className = "navi";
        }
    }
</script>

